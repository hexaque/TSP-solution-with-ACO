function [model] = createmodel()


model = [];

n=81;           // şehir sayısı

D = zeros(n,n); 


model.D = deger();   // iller arası tüm uzaklık değerlerini değişkene fonksiyon yardımı ile atıyoruz.

endfunction
